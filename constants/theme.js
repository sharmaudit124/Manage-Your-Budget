import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");

export default SIZES = {
    font: 14,
    radius: 12,
    padding: 20,
    otherPading: 36,
    largeTitle: 50,
    width,
    height
}